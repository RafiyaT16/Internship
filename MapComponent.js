import React from 'react';

const MapComponent = ({ address }) => {
    return (
        <div style={{ height: '400px', width: '100%' }}>
            {/* Replace with actual map library component */}
            <p>Map showing location for: {address}</p>
        </div>
    );
};

export default MapComponent;
